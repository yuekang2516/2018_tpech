
angular.module('app').factory('machineDataBoardService', machineDataBoardService);
machineDataBoardService.$inject = ['$rootScope', '$http', '$q', 'Hub', '$timeout', 'SettingService'];

function machineDataBoardService($rootScope, $http, $q, Hub, $timeout, SettingService) {
    const rest = {
        
    };

    return rest;
}
